# DeepSeek API配置
api_key = "your_api_key_here"

# 白名单配置
[whitelist]
# 允许使用DeepSeek API的群组ID列表
group_ids = [123456789]
# 允许使用DeepSeek API的用户ID列表
user_ids = [987654321]

# 模型配置
[model]
# 默认模型，可选deepseek-chat或deepseek-reasoner
default = "deepseek-chat"
# 默认温度设置
temperature = 1.0 